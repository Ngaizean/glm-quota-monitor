# 当前仓库

glm-quota-monitor（GLM 额度监控，Tauri 2 + React）。

# 当前主题

GitHub Actions 自动编译的 macOS 安装包，和在自己真实 Mac 上编译，有区别吗？

# LLM讲解

## 先说结论：几乎没有区别

GitHub Actions 的 `macos-latest` runner **就是一台真实的 Mac**，由 GitHub 托管。它不是模拟器、不是虚拟化 Linux，而是苹果硬件上跑的 macOS 环境。

所以：
- 它编译出的 `.dmg` / `.app`，和你在自己 Mac 上 `npm run tauri build` 出来的**本质一样**
- 差别只在"环境配置"和"签名环节"，不在"编译器本身"

## 打个比方

你在自己家厨房做菜 vs 去一个共享厨房做菜：

- 食材、灶台、锅都一样（macOS + 同样的工具链）
- 但共享厨房里，你不一定装了自家柜子里的那罐酱（环境差异）
- 而且做完菜，没人帮你盖章认证（签名/公证）

## 那"区别"具体在哪？

| 维度 | 真实 Mac | GitHub CI |
|---|---|---|
| 编译结果 | 一样 | 一样 |
| 默认芯片架构 | 看你的机器 | `macos-latest` = Apple Silicon (arm64) |
| 出 Intel 包 | 需另配 | 需指定其他 runner 或交叉编译 |
| 签名 / 公证 | 要 Apple Developer 证书 | 同样要证书，没有就都不签 |
| 环境纯净度 | 可能装了一堆杂七杂八 | 每次从零，可复现 |
| GUI 手动验证 | 能点按钮测 | 不行，无头环境 |
| 保密性 | 你自己控制 | GitHub 托管 |

## 关键理解点

1. **"自动编译"不是另一种编译**——只是编译发生在别人的 Mac 上，而不是模拟
2. **真正的差异是"环境"和"证书"**，不是"产物质量"
3. **无头 = 跑不了 GUI 交互测试**，这是 CI 最大的限制

## 检查题

1. 如果你想用 CI 出一个能在 Intel Mac 上运行的包，需要做什么？（提示：看表格第 3 行）
2. 为什么 CI 编译的包放到别人电脑上可能打不开？（提示：看签名那一行）

---
*讲解基于 Tauri 官方打包机制与 GitHub Actions macOS runner 的通用事实。*
