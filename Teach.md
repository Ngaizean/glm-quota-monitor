# 当前仓库

glm-quota-monitor（GLM 额度监控，Tauri 2 + React）。我们刚修完了一串 Codex 账号同步问题。

# 当前主题

什么是 refresh token（刷新令牌）？为什么它会让你的 Codex 账号"过期了却救不回来"？

# LLM讲解

## 先看一张"人脸识别"的类比（你最熟的场景）

去银行办事要刷脸（短时凭证，几分钟就废）+ 身份证（长时凭证，几年有效）。

- **刷脸记录** ≈ `access_token`：短命（Codex 的只有 ~10 天），到处举着它办事偶尔能直接验明正身
- **身份证** ≈ `refresh_token`：长命，不直接办事，但能用来"补办一张新刷脸记录"
- 等到刷脸记录失效了，你就拿身份证去换一张新的刷脸记录 → 这叫**刷新 (refresh)**

但 Codex 的身份证有一个特殊规则（代码里写死的事实，见 `src-tauri/src/codex/auth.rs:249`）：

> 注释原文："refresh_token 是**轮转的**，刷新后旧的立即失效"

## 轮转 (rotation) 是什么：一次性身份证

普通身份证换一次，旧证作废。Codex 的 refresh_token 像**火车票**：

- 你拿它去闸机（auth.openai.com/oauth/token，见 auth.rs:245）换新票
- 闸机吐给你：新 access_token + **一张全新的 refresh_token**
- **你手里的旧票立刻作废**

所以真正的刷新成功后，你手里永远只有"最新一张"refresh_token。

## 灾难现场：你是怎么踩上的

你的账号时间线（今天实测解码出的数据）：

1. **08/26** 登录 → 拿到 access_token + refresh_token 牢牢攥在手里
2. **09/05 22:42** access_token 到期。某个时刻（应用自动刷新、或你手动操作）拿手里的 refresh_token 去换新票 —— 换成功了，服务器作废了旧 refresh_token，发了新的一张
3. 但是 gist 里分发的、以及后来各台机器上存的，还是**旧的那张 refresh_token**
4. 现在旧的一张已经"用过一次"，服务器认定它作废 → 我们再拿它去换，服务器回答：`refresh token was already used`（它就是那句愿意：这票用过了）

**一句话：不是应用没写好，是"分发的车票已经检过一次票了"。**

## 设计原因：为什么故意这么折腾？

安全平衡。如果 refresh_token 不轮转：

- 泄露一次 = 攻击者永久控制账号（长期有效还不用更新）
- 轮转后：**令牌用过就废**，即使被偷，偷到的是旧票，换个时间点就失效

代价就是你现在经历的：**"复制粘贴令牌到别处共享"这套玩法，天生和"一次性车票"冲突**——你共享给别人的那一刻，旧票上所有副本还是有用的；但只要谁拿着它成功刷新过一次，其余所有副本全部作废。

## Open Questions

- 你觉得，如果令牌**不**轮转，对"群里 20 个人分享一个号"的场景是不是更好用？轮转到底在保护什么？
- 检查题（解答发我）：现在这台机器要恢复使用，有哪两条路？为什么"用旧 refresh_token 刷新"一定失败？

# Q: 是否应该让分发方"同时更新 refresh token"？

**方向对了，但这句话里藏着一个反直觉陷阱。**

### 陷阱：刷新是"源头事件"，不是"复制事件"

refresh_token 的**新值**只有在你执行一次刷新时才产生。所以：

- 分发方想"更新 refresh token" → 他必须**先刷新一次**
- 他一刷新 → 服务器立即作废 gist 里那张旧票（=你手里那张）
- 他再把**新的** refresh + access 传上去 → 这时你才拿到能用的新票

所以"同时更新 refresh"没问题，**问题是谁在什么时候消费（refresh）**。正确的图是这样：

```
源头机器 A（唯一允许 refresh 的人）
 │  登录账号 → 拿到 access + refresh
 │  从不共享 refresh，只把 access 复制出去
 ▼
gist ──► 接收者们（只用 access，10 天内够用）
 │
 └─ access 快到期 → 只有 A 去 refresh（一次性拿到新 access+新 refresh）
                      → 立刻把新 access 再复制出去
                      → 旧的 refresh 又作废，但没人需要它了
```

### 反之，最糟的模型（你现在差点走进的坑）

如果**接收者也默认"顺手 refresh 一下"**：

```
B 拿新 refresh 去刷新成功 → 服务器又作废了 A 传给 C、D… 的所有副本
```

共享的 refresh token 对**多副本分发**是毒药：它注定只能有一个"活跃消费者"。谁在源头之外碰它，谁就让所有其他副本陪葬。

### 所以这句该改成：

> 分发方应该保证——**上传到 gist 的 refresh token 一定是"最新的、没被别人消费过的"**，
> 而且**刷新这个动作永远只发生在源头（登录机）**，接收者拿到什么用什么，绝不 refresh。

这正是我们 `force_apply=true` 设计 + "分发账号只读 apply" 背后的同一层逻辑：**共享读，独家写**。

### 检查题（这轮答对才算过关）

1. 如果分发方 A 照着错误模型做：A 刷新了一次拿到新 refresh，传上 gist，但**A 自己的 codex CLI 又开着自动刷新**。10 天后会发生什么？
2. 接收者 B 拿到有效 access（还有 9 天），B 手贱 refresh 了。此时 C 手上的令牌会怎样？B 自己呢？
3. 用一句话总结"多副本分发 refresh token"的死穴。

---
*讲解基于真实代码：`auth.rs:245-314`（刷新请求与轮转注释）、`types.rs:136`（JWT exp 解码）、今天实测的 iat=08/26 / exp=09/05 数据。*